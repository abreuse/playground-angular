import { DifferentNamesDirective } from './different-names.directive';

describe('DifferentNamesDirective', () => {
  it('should create an instance', () => {
    const directive = new DifferentNamesDirective();
    expect(directive).toBeTruthy();
  });
});
